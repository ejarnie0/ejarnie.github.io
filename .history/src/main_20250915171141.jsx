function BookCard() {
    return (
        <div className="book-card">
            <img>(props.image)</img>
            <h2 className="book-title">(props.title)</h2>
            <h3 className="book-author">(props.title)</h3>
            <button className="add-button">(props.title)</button>
        </div>
    )
}