function BookCard() {
    return (
        <div className="book-card">
            <img>(props.image)</img>
            <h1 className="book-title">(props.title)</h1>
            <h2 className="book-author">(props.title)</h2>
            <button className="add-button">(props.title)</button>
        </div>
    )
}