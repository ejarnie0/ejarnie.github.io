function BookCard() {
    return (
        <div className="book-card">
            <img>(props.image)</img>
            <h2>(props.title)</h2>
            <h3>(props.title)</h3>
            <a className="add-button">(props.title)</a>
        </div>
    )
}